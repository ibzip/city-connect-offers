{
  "name": "@city-wallet/db",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "exports": {
    ".": "./src/index.ts"
  },
  "scripts": {
    "build": "tsc -p tsconfig.json --noEmit",
    "typecheck": "tsc -p tsconfig.json --noEmit",
    "lint": "tsc -p tsconfig.json --noEmit"
  },
  "dependencies": {
    "@city-wallet/contracts": "workspace:*",
    "@city-wallet/data-seed": "workspace:*",
    "@city-wallet/utils": "workspace:*",
    "@prisma/client": "^5.22.0"
  }
}
