{
  "name": "@city-wallet/analytics-domain",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "exports": {
    ".": "./src/index.ts"
  },
  "scripts": {
    "build": "tsc -p tsconfig.json --noEmit",
    "typecheck": "tsc -p tsconfig.json --noEmit",
    "lint": "tsc -p tsconfig.json --noEmit",
    "test": "node --import tsx --test src/*.test.ts"
  },
  "dependencies": {
    "@city-wallet/contracts": "workspace:*",
    "@city-wallet/db": "workspace:*",
    "@city-wallet/utils": "workspace:*"
  }
}
