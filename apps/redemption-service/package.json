{
  "name": "@city-wallet/redemption-service",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "scripts": {
    "build": "tsc -p tsconfig.json --noEmit",
    "typecheck": "tsc -p tsconfig.json --noEmit",
    "lint": "tsc -p tsconfig.json --noEmit"
  },
  "dependencies": {
    "@city-wallet/db": "workspace:*",
    "@city-wallet/redemption-domain": "workspace:*",
    "@types/aws-lambda": "^8.10.147"
  }
}
