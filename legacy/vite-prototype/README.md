This folder contains the original AI-generated Vite/Lovable prototype as a reference only.

The real demo product surfaces now live in:

- `apps/consumer-wallet`
- `apps/merchant-portal`

This legacy folder is excluded from the pnpm workspace and root TypeScript build.
