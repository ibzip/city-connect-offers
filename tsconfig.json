{
  "extends": "./tsconfig.base.json",
  "compilerOptions": {
    "noEmit": true
  },
  "include": [
    "apps/**/*.ts",
    "apps/**/*.tsx",
    "packages/**/*.ts",
    "packages/**/*.tsx",
    "infra/**/*.ts",
    "prisma/**/*.ts"
  ],
  "exclude": ["node_modules", "legacy", ".next", "out", "dist"]
}
